{
  "name": "bingo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /bingoJS/ && gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist build .cache gh-pages-clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.10.0",
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4",
    "stylelint": "^13.7.2",
    "stylelint-config-standard": "^20.0.0"
  }
}
